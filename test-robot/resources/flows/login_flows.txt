*** Keywords ***
Successful Login Flow
    [Arguments]    ${user}
    Start Browser Session
    Go To Login
    Should Not Be Logged In
    Login As    ${user}
    Should Be Logged In As    ${user}
    Logout
    Close Browser

Failed Login Flow
    [Arguments]    ${user}
    Start Browser Session
    Go To Login
    Login As    ${user}
    Close Browser